# 相关术语

**循迹：**指船舶按照预设航线自动航行的功能。

**航线：**由多个路径点按顺序连接形成的航行路径，可用于循迹任务。

**路径点：**航线中的基础节点，用于定义船舶航行路径的关键位置。

**电子围栏：**在地图上定义的安全区域边界，用于限制船舶航行范围。

**越界报警：**当船舶驶出电子围栏区域时触发的声光提示机制。

**靠泊：**指船舶靠近并对准泊位区域的操作过程。

**泊位：**用于船舶停靠的目标区域，在系统中以矩形区域表示。
